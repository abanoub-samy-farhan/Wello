import React from 'react';
import {User, PaymentMethod} from '../interfaces';

export default function InformationEntry({ user} ) {
    return (
        <div>
            <h1>Information Entry</h1>
            <h2>Let's get started</h2>
        </div>

    );
}